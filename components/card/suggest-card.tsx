import { IFolderAndFile } from '@/types'

interface SuggestCardProps {
  item: IFolderAndFile
}

const SuggestCard = ({item}: SuggestCardProps) => {
  return <div>SuggestCard</div>
}

export default SuggestCard
